let v1= Number(prompt("give me a number")) // here the user will give one value
                                           // if sometimes the user giving something instead of number ,thn it will convert to number type (because here we using number constructor)
    confirm(`you have give ${v1}`)         // here we will get one pop-up notification                              

let v2= Number(prompt("give me a number"))
 confirm(`you have give ${v2}`) 

 console.log(v1)
 console.log(v2)

 document.write(`the summation of given number is ${v1+v2}`)  // here it will print in the UI/ webpage

 